{
  "name": "organic-new-tab-page",
  "version": "1.0.0",
  "description": "",
  "main": "background.js",
  "scripts": {
    "clean": "rm -rf dist",
    "copy": "mkdir -p dist && cp manifest.json newtab.html styles.css dist/ && cp -r icons dist/",
    "compile": "tsc",
    "build": "npm run clean && npm run copy && npm run compile"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@types/chrome": "^0.1.37",
    "typescript": "^5.9.3"
  }
}
